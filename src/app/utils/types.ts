export interface Achievement {
    icon: string;
    text: string;
  }
  
  export interface PiitramData {
    images: string[];
    honors: string[];
    gains: string[];
    achievements: Achievement[];
  }